[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # VITE_ vars are intentionally bundled into the client JS.
  # The password gate provides access control; do not put paid provider secrets in VITE_ vars.
  SECRETS_SCAN_OMIT_KEYS = "VITE_LLM_API_KEY,VITE_SITE_PASSWORD"
