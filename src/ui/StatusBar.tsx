// Bottom status bar: zoom level, fragment count, cluster count, online indicator. Implemented in Session 02.
export default function StatusBar(_props: { zoom: number; fragmentCount: number; clusterCount: number }) {
  return null;
}
