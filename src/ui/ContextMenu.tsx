// Fragment hover contextual menu (fact-check, pivot, star, delete). Implemented in Session 02.
export default function ContextMenu(_props: { fragmentId: string; onClose: () => void }) {
  return null;
}
