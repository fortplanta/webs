import { Cluster as ClusterType } from '../api/types';

// Cluster wrapper + fragment positioning. Implemented in Session 02.
export default function Cluster(_props: { cluster: ClusterType; zoom: number }) {
  return null;
}
