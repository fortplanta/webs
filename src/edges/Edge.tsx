import { Edge as EdgeType, Cluster } from '../api/types';

// SVG edge between two clusters. Implemented in Session 02.
export default function Edge(_props: { edge: EdgeType; clusters: Cluster[] }) {
  return null;
}
