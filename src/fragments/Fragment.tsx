import { Fragment as FragmentType } from '../api/types';

// Master fragment component. Implemented in Session 02.
export default function Fragment(_props: { fragment: FragmentType }) {
  return null;
}
