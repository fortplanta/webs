import { Fragment } from '../../api/types';

// vertical-flow layout (concept, thesis, era, domain). Implemented in Session 02.
export default function VerticalFlow(_props: { fragment: Fragment }) {
  return null;
}
